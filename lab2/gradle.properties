# JVM 设置,Android Studio 启动时也会用
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true

# Kotlin
kotlin.code.style=official

# 非传递型 R class(更快构建)
android.nonTransitiveRClass=true
